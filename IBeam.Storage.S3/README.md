# IBeam.Storage.S3

AWS S3 provider for `IBeam.Storage.Abstractions`.

Registration:

```csharp
builder.Services.AddIBeamS3BlobStorage(builder.Configuration);
```

Configuration section: `IBeam:Storage:S3`
