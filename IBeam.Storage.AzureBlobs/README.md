# IBeam.Storage.AzureBlobs

Azure Blob Storage provider for `IBeam.Storage.Abstractions`.

Registration:

```csharp
builder.Services.AddIBeamAzureBlobStorage(builder.Configuration);
```

Configuration section: `IBeam:Storage:AzureBlobs`
