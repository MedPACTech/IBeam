trigger:
  branches:
    include:
    - main

pool:
  vmImage: 'ubuntu-22.04'  # ✅ REQUIRED to select a valid image

variables:
  buildPlatform: 'AnyCPU'
  buildConfiguration: 'Release'
  solution: '**/IBeam.sln'
  project: '**/IBeam.Services.csproj'
  packageLocation: 'https://nuget.pkg.github.com/MedPACTech/index.json'
  apiKey: '$(GH_TOKEN)'  # ✅ Recommended to store your GitHub PAT securely

steps:
- task: NuGetToolInstaller@1
  displayName: 'Install NuGet'

- task: NuGetCommand@2
  displayName: 'Restore Packages'
  inputs:
    command: 'restore'
    restoreSolution: '$(solution)'  # ✅ Use .sln file for restore
    feedsToUse: 'select'

- task: DotNetCoreCLI@2
  displayName: 'Build Project'
  inputs:
    command: 'build'
    projects: '$(project)'
    arguments: '--configuration $(buildConfiguration)'

- task: DotNetCoreCLI@2
  displayName: 'Pack NuGet'
  inputs:
    command: 'pack'
    packagesToPack: '$(project)'
    packDirectory: '$(Build.ArtifactStagingDirectory)'

- script: |
    dotnet nuget push "$(Build.ArtifactStagingDirectory)/**/*.nupkg" --source $(packageLocation) --api-key $(apiKey) --skip-duplicate
  displayName: 'Push NuGet Package'
