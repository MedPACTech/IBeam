# IBeam.Storage.FileSystem

Local file-system provider for `IBeam.Storage.Abstractions`.

Registration:

```csharp
builder.Services.AddIBeamFileSystemBlobStorage(builder.Configuration);
```

Configuration section: `IBeam:Storage:FileSystem`
