trigger:
  branches:
    include:
    - main
    - development
    - release/*
    - feature/*
    - bugfix/*
  tags:
    include:
    - v*
  paths:
    include:
    - IBeam.Communications.Email.AzureCommunications/**
    - .azure-pipelines/pipelines.IBeam.Communications.Email.AzureCommunications.nupkg.yml

pool:
  vmImage: 'ubuntu-22.04'

variables:
- group: GitHubNuGetSecrets

- name: buildPlatform
  value: 'AnyCPU'
- name: buildConfiguration
  value: 'Release'
- name: solution
  value: 'IBeam.sln'
- name: project
  value: '**/IBeam.Communications.Email.AzureCommunications.csproj'
- name: testProjectPattern
  value: '**/IBeam.Tests.Communications.Email.AzureCommunications.csproj'
- name: packageLocation
  value: 'https://api.nuget.org/v3/index.json'
- name: apiKey
  value: '$(NUGET_API_KEY)'

stages:
- stage: BuildAndPush
  jobs:
  - job: BuildJob
    steps:
    - checkout: self
      clean: true
      fetchDepth: 0        # NBGV needs full history
      fetchTags: true      # needed for tag-based release versions
      lfs: false

    - template: templates/dotnet-nuget-build.yml
      parameters:
        solution: $(solution)
        project: $(project)
        packageLocation: $(packageLocation)
        apiKey: $(apiKey)
        publishPackages: false
        testProjectPattern: $(testProjectPattern)
        buildConfiguration: $(buildConfiguration)






